# [Claude Code 작업 지시] stock_auto 청산 룰 재설계 & 대시보드 보강

> 사용법: 이 파일을 `Stock` 레포 루트에 `TASK.md`로 넣고 Claude Code에서
> `TASK.md 읽고 Phase 0부터 순서대로 진행해. 각 Phase 끝나면 결과 요약하고 멈춰서 확인받아.` 라고 시작.
> 배경 분석은 `docs/analysis_2026-09-18.md` (성과분석 md) 참고.

## 배경 (요약)

- 2026-09-09 ~ 09-17 추천 24건, 청산 12건 성과: 손익 -6.67%, 승률 42%, PF 0.67, 평균 보유 1.0일.
- 원인 진단: 종목 선정은 괜찮음(D+2 단순보유 승률 73%, 같은 12건 D+2 보유 시 +27%). **보유상한 1일 + 고정 % 손절 + SL 쪽 갭 슬리피지**가 수익을 깎음.
- 확신도(0.45~0.58) 변별력 없음. 다만 0.50 미만 4건 전패.
- SWKS 4일 연속 재추천, 반도체 섹터 쏠림, 미체결 2건 사유 미기록.

## 원칙

1. **선정(스코어링) 로직은 건드리지 않는다.** 이번 작업은 청산·실행·계측만.
2. 파라미터는 12건에 맞추지 말고 **과거 시그널 전체 백테스트(n ≥ 100)** 로 결정한다. 부족하면 부족하다고 보고.
3. 기존 대시보드/파이프라인 출력 포맷은 유지하고 컬럼·섹션을 추가하는 방식으로.
4. 각 Phase는 독립 커밋. 테스트 없이 다음 Phase로 넘어가지 않는다.
5. 변경 전 반드시 레포 구조를 먼저 파악하고, 아래 "찾아야 할 것"을 채운 뒤 진행.

## Phase 0 — 코드 파악 (읽기만)

찾아서 보고할 것:
- 추천 시그널 저장 위치/스키마 (날짜, 종목, 라벨 단타/중단기/스윙, 확신도, 진입가)
- 청산 시뮬레이션 코드: TP/SL/보유상한 값이 어디에 하드코딩돼 있는지, 장중 vs 종가 판정인지, 체결가 산정 방식
- D+N / MFE20 / MAE20 계산 코드
- `stock_auto.pipeline.triage` 의 실행/미체결 입력 구조
- 대시보드 HTML 생성 코드
- 과거 시그널이 몇 건 누적돼 있는지 (백테스트 가능 표본 크기)
- 가격 데이터 소스와 ATR 계산 가능 여부 (OHLC 보유 여부)

출력: `docs/codemap_exit.md` 에 파일 경로·함수명 기준으로 정리.

## Phase 1 — 청산 엔진 파라미터화 + 백테스트 하네스

- [ ] `ExitConfig` (dataclass) 도입: `hold_days_by_label: dict`, `sl_mode: "pct"|"atr"`, `sl_value`, `tp_mode`, `tp_value`, `close_based: bool`, `trail_be_atr: float|None`, `partial_tp: float|None`
- [ ] 기존 동작(1일 보유, 고정 % TP/SL, 장중 판정)을 `ExitConfig.legacy()` 로 재현 → **기존 12건 결과가 동일하게 나오는 회귀 테스트** 먼저 통과
- [ ] ATR(14) 계산 유틸 추가 (진입 전일 기준)
- [ ] 체결가 현실화: SL 체결가 = `min(손절가, 해당일 시가)` (갭 반영), TP는 `max(목표가, 시가)` 가 아니라 `min(목표가, 시가)` 로 보수적으로. 옵션 `slippage_bps` 추가.
- [ ] `stock_auto/backtest/exit_grid.py`: 과거 시그널 전체에 대해 그리드 실행
  - hold ∈ {1,2,3,5}, sl ∈ {1.0,1.5,2.0 ATR} ∪ {2%,3%}, tp ∈ {2,2.5,3 ATR} ∪ {3%,5%}, close_based ∈ {T,F}
  - 지표: 건수, 승률, PF, 건당 기대값, MDD, 평균보유일, MFE 포착률(실현/MFE20)
  - 시간 순 walk-forward (앞 60% 선택 → 뒤 40% 검증) 결과를 별도 표기
  - 결과 CSV + 상위 10개 요약 markdown 출력
- [ ] 라벨(단타/중단기/스윙)별로 그리드 결과 분리

멈춤: 그리드 결과 보고 → 사람이 `ExitConfig` 기본값 결정.

## Phase 2 — 실행 필터

- [ ] `min_confidence` (기본 0.50) 미만은 추천 목록엔 남기되 `실행 대상 아님` 플래그
- [ ] 동일 종목 SL 청산 후 `cooldown_days` (기본 3거래일) 재추천 차단 (또는 플래그)
- [ ] 섹터 매핑(기존 섹터 ETF 분석 로직 재사용) → 동시 보유 같은 섹터 `max_per_sector` (기본 2) 초과 시 플래그
- [ ] 필터 적용 전/후 백테스트 지표 비교 표 출력

## Phase 3 — 계측 (triage · 미체결 · 슬리피지)

- [ ] `triage` 입력에 `fill_price`, `fill_status` (`filled|unfilled_gap|unfilled_user|unfilled_liquidity|other`), `note` 추가. 기존 입력 파일과 하위호환.
- [ ] 예상 체결가 vs 실제 체결가 → 슬리피지(bps) 저장, 대시보드에 평균/분포 표시
- [ ] 미체결 건도 "만약 체결됐다면" 반사실 손익을 별도 집계 (놓친 수익 추적)

## Phase 4 — 대시보드 보강

추가할 타일/섹션:
- [ ] 건당 기대값, 평균 익절 / 평균 손실, 실효 손익비
- [ ] MFE 포착률 (실현수익 / MFE20 평균)
- [ ] 분해 표: 라벨별 · 확신도 구간별(0.45/0.50/0.55/0.60+) · 섹터별 — 건수/승률/PF/기대값
- [ ] 에쿼티 곡선에 "D+2 단순보유 반사실" 선 겹치기
- [ ] 실행 필터로 걸러진 건은 목록에 회색 pill로 표시 (`제외: 확신도`, `제외: 쿨다운` 등)
- [ ] 현재 사용 중인 `ExitConfig` 값을 대시보드 상단 sub 라인에 출력

## Phase 5 — 검증 & 문서

- [ ] `tests/`: legacy 회귀, ATR 계산, 갭 체결가, 쿨다운, 섹터 캡 단위 테스트
- [ ] `README` 에 ExitConfig / 필터 파라미터 설명, 백테스트 실행법 추가
- [ ] 마지막 보고: 기존 12건을 새 기본 설정으로 재계산한 결과 vs 기존 결과 비교 (단, "12건 결과가 좋아졌다"를 근거로 쓰지 말고 백테스트 결과를 근거로 쓸 것)

## 완료 기준

- 백테스트(n ≥ 100, walk-forward 검증 구간)에서 PF ≥ 1.3, 실효 손익비 ≥ 1.5, 건당 기대값 > +0.5%
- 표본이 부족하면 "현재 표본으로는 결론 불가, N건 더 필요" 로 명시하고 legacy 설정 유지
- 실거래 금액 확대는 이 작업 범위 밖 (사람이 결정)
